{
  "name": "claimoptiq-erp",
  "version": "1.0.0",
  "description": "ClaimOptiq AI ERP Suite - Insurance Claim Management System",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && CI=false REACT_APP_API_URL=/api npm run build",
    "start": "cd backend && node server.js",
    "seed": "cd backend && node seed.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
